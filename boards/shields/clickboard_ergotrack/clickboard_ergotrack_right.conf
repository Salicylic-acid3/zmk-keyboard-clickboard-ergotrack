CONFIG_I2C=y
CONFIG_INPUT_IQS9151=y
CONFIG_ZMK_POINTING=y

# 電池ゲージ。いまの目盛りは信用できない: 新品の CR2032 x2 (並列・約3.0V) で
# ずっと 1% を指しており、ADC が拾っている電圧が約2.01Vということになる。
# モジュール側は分圧を一切補正せず、ピンの電圧をそのまま電池電圧として扱う
# (binding に output-ohms / full-ohms が無い)。基板側の分圧比が分かるまで
# MIN/MAX は暫定。
CONFIG_ZMK_NON_LIPO_MAX_MV=3000
CONFIG_ZMK_NON_LIPO_MIN_MV=2000

# ここが危険だった。LOW_MV 以下を読むと、モジュールは sys_poweroff() で
# 半身ごと電源を落とす (USB給電中だけ見送る)。それが 2000 = MIN_MV = 実測値
# だったので、電池駆動のときは常に電源断の一歩手前にいた。
# 「左にUSBを挿すとパッドが生き返る」のはこれで説明がつく。
# 目盛りが直るまでは発動しない値にしておく。
CONFIG_ZMK_NON_LIPO_LOW_MV=1200

CONFIG_ZMK_STUDIO=y
# Studio のロックは無効。Keeb-On! Studio へは有線でだけ繋ぐ。
#
# ロックと無線はセットだった。キーボードは接続中アドバタイズを止めるので、
# 普通に使っているキーボードはブラウザのデバイス選択に出てこない。それを
# 回避する ZMK_STUDIO_LOCK_BLE_DIRECT_ADVERTISING_ON_UNLOCK は
# 「default y if ZMK_STUDIO_LOCKING」で、しかも立つのはアンロックの瞬間
# だけ。つまり「無線で繋ぐ」と「ロック無し」は両立しない。
#
# 無線を捨てた方を選んだ。開けられないロックの付いたキーボードは設定できない
# キーボードで、実際に一度そうなった。default は y なので、明示的に n と書く。
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_ZMK_STUDIO_RPC_RX_BUF_SIZE=192
CONFIG_ZMK_STUDIO_RPC_CUSTOM_SUBSYSTEM_REQUEST_PAYLOAD_MAX_BYTES=192
CONFIG_ZMK_LOW_PRIORITY_THREAD_STACK_SIZE=2048

# --- Custom Studio Protocol: connection management ---
# (Keeb-On! Studio's Connection tab: BLE profile naming/switching/unpairing,
# per-connection and per-OS default layers.)
CONFIG_ZMK_CUSTOM_SETTINGS=y
CONFIG_ZMK_CUSTOM_SETTINGS_STUDIO_RPC=y

CONFIG_ZMK_BLE_MANAGEMENT=y
CONFIG_ZMK_BLE_MANAGEMENT_STUDIO_RPC=y

CONFIG_ZMK_OS_DETECTION=y
CONFIG_ZMK_OS_DETECTION_USB=y
CONFIG_ZMK_OS_DETECTION_BLE=y
CONFIG_ZMK_OS_DETECTION_STUDIO_RPC=y

CONFIG_ZMK_DEFAULT_LAYER=y
CONFIG_ZMK_DEFAULT_LAYER_MAX_INDEX=1
CONFIG_ZMK_DEFAULT_LAYER_STUDIO_RPC=y
CONFIG_ZMK_DEFAULT_LAYER_OS_DETECTION=y

# --- Custom Studio Protocol: macro & combo runtime editing ---
# (Keeb-On! Studio's Macro&Combo tab.)
CONFIG_ZMK_RUNTIME_MACRO=y
CONFIG_ZMK_RUNTIME_MACRO_STUDIO_RPC=y
CONFIG_ZMK_BEHAVIOR_LOCAL_ID_TYPE_CRC16=y
CONFIG_ZMK_CUSTOM_SETTINGS_LARGE_VALUE_MAX_SIZE=256

CONFIG_ZMK_RUNTIME_COMBO=y
CONFIG_ZMK_RUNTIME_COMBO_STUDIO_RPC=y

# --- Custom Studio Protocol: diagnostics & device settings ---
# (Keeb-On! Studio's Troubleshooting and Settings tabs.)
CONFIG_ZMK_DEVICE_INFO=y
CONFIG_ZMK_DEVICE_INFO_STUDIO_RPC=y

CONFIG_ZMK_WATCHDOG=y
CONFIG_ZMK_WATCHDOG_STUDIO_RPC=y

CONFIG_ZMK_KSCAN_DIAGNOSTICS=y
CONFIG_ZMK_KSCAN_DIAGNOSTICS_STUDIO_RPC=y
CONFIG_ZMK_KSCAN_DIAGNOSTICS_SPLIT=y
# Required on BOTH halves: a peripheral reply carries a full topology page,
# which is larger than ZMK's default relay payload.
CONFIG_ZMK_SPLIT_RELAY_EVENT_DATA_LEN=256

# Live key-press view during the kscan test (and in the keymap editor).
CONFIG_ZMK_INPUT_STREAM_FEATURE=y
CONFIG_ZMK_INPUT_STREAM_FEATURE_STUDIO_RPC=y

CONFIG_ZMK_SETTINGS_RPC=y
CONFIG_ZMK_SETTINGS_RPC_STUDIO=y

# --- キーマップの高速読み込み ---
# 読み取り専用。フィンガープリントで差分だけ取るので、BLE接続時の
# キーマップ表示が速くなる。編集・保存は従来どおり公式プロトコル。
# 要求サイズは小さく、RX_BUF は上で192にしてあるので追加調整は不要。
CONFIG_ZMK_FAST_KEYMAP=y
CONFIG_ZMK_FAST_KEYMAP_STUDIO_RPC=y

# --- キー以外の部品(トラックパッド)の位置 ---
# 公式プロトコルはキーしか送らないので、パッドの位置はこのモジュールが送る。
# ノード定義は clickboard_ergotrack_right.overlay の末尾。
CONFIG_ZMK_PHYSICAL_LAYOUTS_FEATURE=y
CONFIG_ZMK_PHYSICAL_LAYOUTS_FEATURE_STUDIO_RPC=y

# --- トラックパッドのランタイム調整 ---
# 基準の感度をアプリから設定する。キーに割り当ててある zip_dyn_scale は
# その場での微調整として残してあるので、両者は掛け算になる。
# CONFIG_ZMK_LOW_PRIORITY_THREAD_STACK_SIZE=2048 が必須(上で設定済み)。
# これが無いとアプリの「一覧を更新」でMPUのスタックガードに当たって落ちる。
# 共有スレッドのスタックはモジュール側からは広げられないので、ここで指定する。
CONFIG_ZMK_RUNTIME_INPUT_PROCESSOR=y
CONFIG_ZMK_RUNTIME_INPUT_PROCESSOR_STUDIO_RPC=y
# processor-label の既定は7文字まで(NUL込み8)。"mouse (alt)" のような
# 読める名前を付けたいので広げる。名前はRPCで送られるだけでなく
# 保存済み設定のキーでもあるので、一度決めた名前は変えないこと。
CONFIG_ZMK_RUNTIME_INPUT_PROCESSOR_NAME_MAX_LEN=16

# Diagnostics responses are large; a bigger TX buffer transfers them with
# fewer stalls.
CONFIG_ZMK_STUDIO_RPC_TX_BUF_SIZE=256

CONFIG_INPUT_IQS9151_DYNAMIC_FILTER_BOTTOM_SPEED=10
CONFIG_INPUT_IQS9151_DYNAMIC_FILTER_BOTTOM_BETA=100
CONFIG_INPUT_IQS9151_CURSOR_INERTIA_ENABLE=n
CONFIG_INPUT_IQS9151_SCROLL_INERTIA_ENABLE=n
# --- ICの解像度: パッドの辺の比に合わせる ---
# 理由は左の .conf に書いてある。要点だけ: 電極の実寸は 85.7 x 52.1 mm で、
# 1300/1200 だと長辺のカウント/mm が短辺の 2/3 しかない。軸の判定は全部
# カウントの比較なので、カーソルの速さだけでなく判定そのものが傾く。
# 1200 x 85.7/52.1 = 1974 で両軸とも 23.0 カウント/mm。
CONFIG_INPUT_IQS9151_RESOLUTION_X=1974
CONFIG_INPUT_IQS9151_RESOLUTION_Y=1200

# --- カーソルの速さは**こちら**。解像度ではない ---
# 実機で3回焼いて確かめた: RESOLUTION_X を 1300 → 1974 にしても、両半身とも
# カーソルは一切変わらなかった。CIの .config も読んで値が入っていることは
# 確認済み。**ICの Relative X/Y は解像度レジスタを通っていない。**
# 上の解像度が効くのは指の絶対座標＝ジェスチャーの軸判定のほうだけ。
#
# なので速さはドライバで直接掛ける。10 が等倍、16 で 1.6倍。
# センサのX軸 = 画面の縦(listener の XY_SWAP のため)。パッドは縦87mm・横53mm で
# 縦のほうが電極ピッチが粗く、そのうえ**縦は一息で端まで撫でられない**ので、
# 辺の比(1.64)より少し強めが具合がいいはず。まず 16 から。
#
# これもアプリから変えられる(keebon__trackpad の cursor_gain_x / _y)。
# 下の2行はその既定値。トラックパッドタブで指の感触に合わせて振ること。
CONFIG_INPUT_IQS9151_CURSOR_GAIN_X_X10=16
CONFIG_INPUT_IQS9151_CURSOR_GAIN_Y_X10=10

# --- スタック: リレーイベント肥大化への対応 ---
# CONFIG_ZMK_SPLIT_RELAY_EVENT_DATA_LEN=256 にしたことで
# struct zmk_split_transport_peripheral_event が約260バイトに膨らんでいる。
# この構造体は split の受け渡しで値渡し・ローカル変数として積まれるため、
# ペリフェラルからイベントが飛ぶたびにスタックを大きく消費する。
CONFIG_BT_RX_STACK_SIZE=2048
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048
CONFIG_MAIN_STACK_SIZE=2048
CONFIG_INPUT_THREAD_STACK_SIZE=2048

# --- クラッシュ解析用: スレッド名とMPUによるスタック保護 ---
# 破損したPCで飛ぶ代わりに「どのスレッドが溢れたか」で止まるようになる。
CONFIG_THREAD_NAME=y
CONFIG_THREAD_STACK_INFO=y
CONFIG_HW_STACK_PROTECTION=y

# --- スワイプの向き(2本指・3本指とも) ---
# 右パッドが基準。左だけ反転させてこちらに合わせてある(左の .conf を見ること)。
# ここに設定は要らないが、対になっている話なので位置だけ書いておく。

# --- 2本指の横スワイプ(戻る/進むなどに割り当て) ---
# 左右とも同じ挙動にする。詳しい理由は左の .conf に書いてある。
CONFIG_INPUT_IQS9151_2F_SWIPE_ENABLE=y

# --- 3本指スワイプ: 横だけしきい値を下げる ---
# 画面の横 = センサのY軸 = パッドの短辺(52.1mm)。3本の指が並ぶだけで
# ほとんど埋まるので、縦と同じ距離を要求すると横だけが極端に難しくなる。
# 詳しい理由は左の .conf に書いてある。
CONFIG_INPUT_IQS9151_3F_SWIPE_THRESHOLD_Y=140

# --- 2本指スクロールとピンチの取り違え対策 ---
CONFIG_INPUT_IQS9151_2F_PINCH_DOMINANCE_X10=20

# --- 斜めに伸ばした指でも「上下スクロール」として扱う ---
# スクロールが始まった瞬間に勝っている軸へ固定する。12 = 相手の1.2倍、
# つまり真っ直ぐから約40度まで。詳しい理由は左の .conf に書いてある。
CONFIG_INPUT_IQS9151_2F_SCROLL_AXIS_LOCK_X10=12

# --- 片手ピンチ(2本指ズーム)— アプリから切り替えられる ---
# 詳しい理由は左の .conf に書いてある。下の2行は**既定値**。
CONFIG_INPUT_IQS9151_2F_PINCH_ENABLE=n
CONFIG_INPUT_IQS9151_2F_PINCH_INVERT=y

# こちらはセントラルなので、Studioサブシステムの登録も入る(既定で y)。
# 登録が無いとカスタム設定は ListSettings から黙って落ちてアプリに見えない。
CONFIG_ZMK_CUSTOM_SETTINGS_SPLIT_RPC_RELAY=y
CONFIG_INPUT_IQS9151_RUNTIME_SETTINGS=y
# listener の zip_scroll_transform XY_SWAP はホイールイベント全部に効くので、
# ピンチの縦ホイールが横スクロールに化けてズームしない。横で出して打ち消す。
CONFIG_INPUT_IQS9151_2F_PINCH_WHEEL_HORIZONTAL=y
# ホイールは zip_scroll_scaler 1 16 で1/16されるので、そのぶんゲインを上げる。
CONFIG_INPUT_IQS9151_2F_PINCH_WHEEL_GAIN_X10=100

# 右パッドのタッチ状態。dual_pad が「左右とも1本ずつ触れている」を判定するのに
# 両側の状態が要る。
CONFIG_INPUT_IQS9151_TOUCH_STATE_ENABLE=y

# --- 左右をまたぐ打鍵の順序 ---
# 「る」が「うr」になる、つまり左→右と速く打つと右が先に届く問題。
# 右手側がセントラルなので、右のキーは自分で即座に処理される。左のキーは
# BLEを渡ってくる。その差が打鍵間隔より大きいと順番が入れ替わる。
#
# 効くのはスレーブレイテンシ。ZMK既定の 30 は「ペリフェラルは接続イベントを
# 最大30回まで寝過ごしてよい」という意味で、間隔 6 (=7.5ms) と掛けると
# 最悪 200ms 以上になる。電池のための設定で、打鍵の遅延そのもの。
# 0 にすると毎回起きるので遅延は 7.5ms 程度に収まる。
#
# 代償は左手側の電池。0 で寿命が気になるようなら 5 くらいまで戻して、
# それでも順序が入れ替わらないかを見る(最悪 45ms 程度)。
CONFIG_ZMK_SPLIT_BLE_PREF_LATENCY=0
# 間隔はすでに最小(6 = 7.5ms)。明示しておく。
CONFIG_ZMK_SPLIT_BLE_PREF_INT=6

# --- タップダンス（アプリから編集可能） ---
# タップ回数ごとの割り当てと判定時間を settings に置く。スロットは最初は
# 空で、アプリで設定するまで押しても何も起きない。
CONFIG_ZMK_RUNTIME_TAP_DANCE=y
CONFIG_ZMK_RUNTIME_TAP_DANCE_COUNT=4
CONFIG_ZMK_RUNTIME_TAP_DANCE_MAX_TAPS=3

# --- 電池残量の報告 ---
# 左右それぞれの残量をカスタム設定として公開する。ZMK本体の報告は
# BLEのBattery Service経由なので、USBで繋いでいるアプリからは見えない。
CONFIG_ZMK_BATTERY_REPORT=y
# セントラルが相方の残量を取り寄せる。これが無いと左手側の値は存在しない。
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
# 残量はTEMPORARYモードで書く(フラッシュに触らない・未保存扱いにもならない)。
# そのスロットを2つ占有し続けるので、既定の2のままだと他が使えなくなる。
CONFIG_ZMK_CUSTOM_SETTINGS_TEMP_SLOTS=4

# --- USB 識別子 ---
# Kconfig.defconfig ではなくここに置いてある。あちらが与えられるのは Kconfig の
# "default" で、同じ記号に default が複数あると **最初に読まれたもの** が勝つ。
# ZMK は app/Kconfig で自分の既定値(VID 0x1D50 / PID 0x615E / "ZMK Project")を
# 先に宣言しているので、シールドの Kconfig.defconfig に書いても一度も効かない。
# 実際 ioreg では 0x1D50 / "ZMK Project" のまま出ていた。
# .conf の代入は default ではなく設定そのものなので、確実に勝つ。
#
# VID 0x355D は使用許諾を得て使用(登録は moimate inc.)。PID はキーボードごと。
# 採番表は進捗ログに。**VID はアプリの接続判定そのもの**で、これが 0x355D で
# ないと Keeb-On! Studio のポート選択に出てこない。
CONFIG_USB_DEVICE_VID=0x355D
CONFIG_USB_DEVICE_PID=0x1028
CONFIG_USB_DEVICE_MANUFACTURER="Keeb-On!"
CONFIG_USB_DEVICE_PRODUCT="ClickBoard ErgoTrack"
